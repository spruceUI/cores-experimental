diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9707ab6..12541c4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -900,12 +900,25 @@ set(PLAYER_BUILD_LIBLCF_BRANCH "master"
 if(PLAYER_BUILD_LIBLCF)
 	# liblcf is built as part of this cmake file
 	set(LIBLCF_PATH "${CMAKE_CURRENT_SOURCE_DIR}/lib/liblcf")
+	find_package(Git REQUIRED)
 	if(NOT EXISTS ${LIBLCF_PATH})
-		find_package(Git REQUIRED)
-		execute_process(COMMAND ${GIT_EXECUTABLE} clone "--depth=1" "--branch"
-			"${PLAYER_BUILD_LIBLCF_BRANCH}"
+		execute_process(COMMAND ${GIT_EXECUTABLE} clone
 			"${PLAYER_BUILD_LIBLCF_GIT}"
 			"${LIBLCF_PATH}")
+		execute_process(COMMAND ${GIT_EXECUTABLE} -C "${LIBLCF_PATH}"
+			-c advice.detachedHead=false checkout
+			666e6c023696d4a45a67dd9ba879dbff7b0f69f3
+			RESULT_VARIABLE PLAYER_LIBLCF_CHECKOUT)
+		if(NOT PLAYER_LIBLCF_CHECKOUT EQUAL 0)
+			message(FATAL_ERROR "pinned liblcf checkout failed")
+		endif()
+	endif()
+	execute_process(COMMAND ${GIT_EXECUTABLE} -C "${LIBLCF_PATH}"
+		rev-parse "HEAD^{tree}"
+		OUTPUT_VARIABLE PLAYER_LIBLCF_TREE
+		OUTPUT_STRIP_TRAILING_WHITESPACE)
+	if(NOT PLAYER_LIBLCF_TREE STREQUAL "4aaae9a4dadefc46011715ebcff2313cf33c0816")
+		message(FATAL_ERROR "pinned liblcf tree mismatch: ${PLAYER_LIBLCF_TREE}")
 	endif()
 	list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/lib/liblcf/builds/cmake/Modules")
 
